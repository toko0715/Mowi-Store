package com.miempresa.mowimarket.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales MOWI - Naranja vibrante
val OrangePrimary = Color(0xFFFF6B35)  // Naranja vibrante principal
val OrangeLight = Color(0xFFFF8C61)    // Naranja claro
val OrangeDark = Color(0xFFE85A2C)     // Naranja oscuro

// Colores secundarios
val OrangeAccent = Color(0xFFFFB84D)   // Amarillo-naranja para acentos
val CoralPink = Color(0xFFFF8566)      // Coral rosado
val WarmGray = Color(0xFFF5F5F5)       // Gris cálido para fondos

// Colores de estado
val SuccessGreen = Color(0xFF4CAF50)   // Verde para éxito
val ErrorRed = Color(0xFFF44336)       // Rojo para errores
val WarningAmber = Color(0xFFFFA726)   // Ámbar para advertencias

// Colores de texto
val TextPrimary = Color(0xFF2C2C2C)    // Texto principal (casi negro)
val TextSecondary = Color(0xFF757575)  // Texto secundario (gris)
val TextOnOrange = Color(0xFFFFFFFF)   // Texto sobre naranja (blanco)

// Colores de fondo
val BackgroundLight = Color(0xFFFFFBF7) // Fondo cálido claro
val SurfaceWhite = Color(0xFFFFFFFF)    // Blanco puro para cards
val SurfaceElevated = Color(0xFFFFF9F5) // Superficie elevada con tinte naranja
