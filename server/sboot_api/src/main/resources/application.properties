spring.application.name=sboot_api

# application.properties - Configuraci�n de Spring Boot para Ecommerce MOWI

# Puerto de la aplicaci�n
server.port=8080

# Configuraci�n de la base de datos (ajusta seg�n tu conexi�n)
spring.datasource.url=jdbc:mysql://localhost:3306/mowi_store
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuraci�n de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.root=INFO
logging.level.com.mowi.ecommerce=DEBUG

# Configuraci�n de CORS (ya habilitado en controllers con @CrossOrigin)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*
spring.web.cors.allowed-headers=*

# Configuraci�n de Jackson (JSON)
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=America/Lima





gemini.api.key=fuck u proyecto integrador

# Modelo a usar
gemini.model=gemini-1.5-flash

# URL de la API (no cambiar)
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models


stripe.api.key=q miran sapos wwwwwww